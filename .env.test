# .env.test

# This file is used for configuring the testing environment.
# It overrides the default .env file when running tests.

# To run tests with PostgreSQL:
# 1. Ensure you have a running PostgreSQL server.
# 2. Update the credentials below to match your test database setup.
# 3. Set `USE_POSTGRES_FOR_TESTS=true` in your environment or this file.

# To run tests with SQLite (in-memory):
# 1. Set `USE_POSTGRES_FOR_TESTS=false` or leave it unset.

# --- Test Database Configuration ---
USE_POSTGRES_FOR_TESTS=false # Set to true to use PostgreSQL for testing

# --- PostgreSQL Test Settings (only used if USE_POSTGRES_FOR_TESTS is true) ---
TEST_POSTGRES_USER=test_user
TEST_POSTGRES_PASSWORD=test_password
TEST_POSTGRES_SERVER=localhost
TEST_POSTGRES_PORT=5433
TEST_POSTGRES_DB_MAIN=pai_nai_dee_test_template

# --- JWT Settings (can be overridden for tests if needed) ---
# SECRET_KEY="a_different_test_secret_key"
# ACCESS_TOKEN_EXPIRE_MINUTES=15
