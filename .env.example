# PostgreSQL
POSTGRES_SERVER=localhost
POSTGRES_USER=your_postgres_user
POSTGRES_PASSWORD=your_postgres_password
POSTGRES_DB=travel_planner_db
POSTGRES_DB_TEST=travel_planner_test_db # For testing

# JWT
SECRET_KEY=your_super_secret_random_key_here
# ACCESS_TOKEN_EXPIRE_MINUTES=30 (Optional, defaults in config.py)
# REFRESH_TOKEN_EXPIRE_DAYS=7 (Optional, defaults in config.py)

# CORS - if you need to override defaults from config.py (comma-separated)
# BACKEND_CORS_ORIGINS="http://localhost:3000,http://localhost:5173"

# Uvicorn (if running directly without main.py's uvicorn.run)
# HOST=0.0.0.0
# PORT=8000
